from setuptools import setup

LIST_OF_REQUIREMENTS = ['streamlit', 'numpy']

setup(
    version="0.0.1",
    packages=["AlternatingLeastSquares"],
    python_requires=">=3.7",
    install_requires=LIST_OF_REQUIREMENTS
)